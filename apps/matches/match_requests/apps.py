from django.apps import AppConfig


class MatchRequestsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.matches.match_requests"
    verbose_name = "Moslik so'rovlari"

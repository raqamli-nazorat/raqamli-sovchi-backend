from django.apps import AppConfig


class PhotoRequestsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.matches.photo_requests"
    verbose_name = "Rasm ko'rish so'rovlari"
